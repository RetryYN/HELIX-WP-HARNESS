export const editorialReviewCriteria=["relevance","clarity","specificity","naturalness","non_duplication"];
const sha256Hex=async(value)=>[...new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(value)))].map((byte)=>byte.toString(16).padStart(2,"0")).join("");
export async function buildEditorialDecisionExport(packet,reviewerIdentifier,decisions){const reviewer=String(reviewerIdentifier??"").trim();if(!reviewer)throw new Error("reviewer identifier is required");if(!packet?.packet_digest)throw new Error("editorial packet is required");if(!decisions?.length)throw new Error("at least one decision is required");return{schema_version:"content-editorial-review-decisions.v1",packet_digest:packet.packet_digest,reviewer_digest:await sha256Hex(`helix-editorial-reviewer.v1\0${reviewer}`),decisions}}
