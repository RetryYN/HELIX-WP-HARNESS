const sha256Hex=async(value)=>[...new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(value)))].map((byte)=>byte.toString(16).padStart(2,"0")).join("");
export async function buildEvidenceSafeRevisionDecisionExport(proposalSetDigest,reviewerIdentifier,decisions){const reviewer=String(reviewerIdentifier??"").trim();if(!reviewer)throw new Error("reviewer identifier is required");if(!/^[a-f0-9]{64}$/u.test(proposalSetDigest??""))throw new Error("proposal set digest is required");if(!decisions?.length)throw new Error("at least one revision decision is required");return{schema_version:"evidence-safe-draft-revision-decisions.v1",proposal_set_digest:proposalSetDigest,reviewer_digest:await sha256Hex(`helix-safe-revision-reviewer.v1\0${reviewer}`),decisions}}
