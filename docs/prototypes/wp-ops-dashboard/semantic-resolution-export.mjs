const sha256Hex=async(value)=>[...new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(value)))].map((byte)=>byte.toString(16).padStart(2,"0")).join("");
export async function buildSemanticResolutionDecisionExport(packetDigest,reviewerIdentifier,decisions){const reviewer=String(reviewerIdentifier??"").trim();if(!reviewer)throw new Error("reviewer identifier is required");if(!/^[a-f0-9]{64}$/u.test(packetDigest??""))throw new Error("semantic decision packet digest is required");if(!decisions?.length)throw new Error("at least one semantic decision is required");return{schema_version:"content-semantic-resolution-decisions.v1",packet_digest:packetDigest,reviewer_digest:await sha256Hex(`helix-semantic-reviewer.v1\0${reviewer}`),decisions}}
