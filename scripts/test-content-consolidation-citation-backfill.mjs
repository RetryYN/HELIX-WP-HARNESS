import assert from "node:assert/strict";
import {buildConsolidationCitationBackfill} from "./content-consolidation-citation-backfill.mjs";

const revision={group_id:"b",sections:[{heading:"IT就活 流れ",heading_candidate_id:"h",paragraphs:[{claim_ids:["b:c"]}]}],claims:[{claim_id:"b:i",claim_kind:"editorial_scope",verification_state:"not_applicable"},{claim_id:"b:c",claim_kind:"observed_search_topic",verification_state:"pending_primary_source"}]},citations=[{citation_id:"x",group_id:"other",url:"https://example.com/a",domain:"example.com",title:"IT就活 流れ",text:"選考",source_keyword:"IT 就活 流れ",organic_url_rank:1}],blueprint={left_group_id:"a",right_group_id:"b",merged_draft_preview:{gate:{missing_citation_source_claim_ids:["b:c"]},claims:[{claim_id:"m",source_claim_ids:["a:c","b:c"]}],citation_recommendations:[{merged_claim_id:"m",url:"https://example.com/a"}]}},result=buildConsolidationCitationBackfill([blueprint],[{group_id:"b",draft_package:{draft_revision:revision}}],citations);
assert.equal(result.rows.length,1);assert.equal(result.rows[0].target_source_claim_id,"b:c");assert.equal(result.rows[0].merged_claim_id,"m");assert.deepEqual(result.rows[0].citation_source_group_ids,["other"]);assert.equal(result.rows[0].duplicates_existing_merged_url,true);assert.equal(result.rows[0].candidate_origin,"cross_group_retained_corpus");assert.equal(result.rows[0].backfill_state,"proposed_unreviewed");assert.equal(result.rows[0].selection_state,"review_only_not_applied");assert.equal(result.rows[0].auto_approval,false);assert.equal(result.rows[0].backfill_digest.length,64);assert.deepEqual(result.summary,{backfill_candidate_count:1,target_source_claim_count:1,unique_url_count:1,cross_group_candidate_count:1,duplicate_existing_merged_url_count:1,unreviewed_count:1,minimum_score:.25,maximum_per_claim:3,selection_state:"review_only_not_applied",auto_approval:false});
console.log("content consolidation citation backfill: OK (cross-group retained corpus, explicit duplicate/provenance, review-only)");
