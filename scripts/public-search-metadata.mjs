import {readFileSync} from "node:fs";
const evidence=JSON.parse(readFileSync(new URL("../docs/research/evidence/public-search-metadata.json",import.meta.url),"utf8"));
const countries=JSON.parse(readFileSync(new URL("../docs/research/evidence/public-search-country-metadata.json",import.meta.url),"utf8"));
const locationRows=[...countries.rows.map((row)=>({...row,granularity:"country",source:"retained_public_metadata",catalog_digest:countries.response_sha256})),...evidence.locations.rows.filter((row)=>row.name!=="Japan").map((row)=>({...row,granularity:"subnational_jp",source:"retained_public_metadata",catalog_digest:evidence.locations.response_sha256}))];
const languageRows=evidence.languages.rows.map((row)=>({...row,source:"retained_public_metadata",catalog_digest:evidence.languages.response_sha256}));
export function searchPublicLocations({locationName="",countryCode=""}={}){const name=String(locationName).trim().toLowerCase(),country=String(countryCode).trim().toUpperCase();if(country&&!/^[A-Z]{2}$/u.test(country))throw new TypeError("countryCode must be an ISO alpha-2 code");return locationRows.filter((row)=>(!name||row.name.toLowerCase().includes(name))&&(!country||row.countryIsoCode===country))}
export function listPublicLanguages(){return languageRows}
export function findPublicLocationName(value){const key=String(value??"").trim().toLowerCase();return key?locationRows.find((row)=>row.name.toLowerCase()===key)??null:null}
export function findPublicLanguageName(value){const key=String(value??"").trim().toLowerCase();return key?languageRows.find((row)=>row.name.toLowerCase()===key)??null:null}
export const publicSearchMetadataSummary={location_count:locationRows.length,country_count:countries.rows.length,jp_location_count:evidence.locations.rows.length,subnational_country_coverage:["JP"],language_count:languageRows.length,country_catalog_digest:countries.response_sha256,jp_location_catalog_digest:evidence.locations.response_sha256,language_catalog_digest:evidence.languages.response_sha256,consumed_credit:countries.consumed_credit+evidence.locations.consumed_credit+evidence.languages.consumed_credit,authentication_required:false,external_acquisition_triggered:false,policy:"retained-public-search-metadata.v2"};
