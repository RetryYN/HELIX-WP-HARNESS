import assert from "node:assert/strict";import {buildCompetitiveAppearanceHistory} from "./competitive-appearance-history.mjs";
const comparison={site_id:"s",group_id:"g",keyword:"kw",previous_task_id:"old",current_task_id:"new",previous_observed_at:"2026-08-01T00:00:00Z",current_observed_at:"2026-08-02T00:00:00Z",elapsed_hours:24,contract_match:true,evidence_digest:"a".repeat(64),url_rank_changes:[{url:"https://a.test/one",domain:"a.test",previous_rank:1,current_rank:3,rank_delta:-2,state:"retained",rank_absence_interpretation:null,title_changed:false,previous_title:"A",current_title:"A"},{url:"https://a.test/two",domain:"a.test",previous_rank:null,current_rank:5,rank_delta:null,state:"entered_observed_depth",rank_absence_interpretation:"outside_observed_set_unknown_rank",title_changed:false,previous_title:null,current_title:"B"},{url:"https://b.test/",domain:"b.test",previous_rank:2,current_rank:null,rank_delta:null,state:"exited_observed_depth",rank_absence_interpretation:"outside_observed_set_unknown_rank",title_changed:false,previous_title:"B",current_title:null}]};
const result=buildCompetitiveAppearanceHistory([comparison]);assert.deepEqual(result.summary.domain_state_counts,{persistent:1,gained:0,lost:1});assert.deepEqual(result.summary.page_state_counts,{persistent:1,gained:1,lost:1});assert.equal(result.summary.domain_history_count,2);assert.equal(result.summary.page_history_count,3);const domain=result.rows.find((row)=>row.entity_type==="domain"&&row.domain==="a.test");assert.equal(domain.previous_best_rank,1);assert.equal(domain.current_best_rank,3);assert.equal(domain.previous_url_count,1);assert.equal(domain.current_url_count,2);assert.equal(domain.shared_url_count,1);assert.equal(domain.best_rank_delta,-2);assert.equal(domain.competitive_use,"stable_competitor_evidence");assert.equal(domain.history_digest.length,64);assert(result.rows.every((row)=>!row.confirmed_unranked&&!row.auto_content_mutation));console.log("competitive appearance history: OK (domain/page persistence, entry, exit and observed-depth caveat)");
