import assert from"node:assert/strict";import{openDashboardDb}from"./keyword-dashboard-db.mjs";import{queryGraphRelatedKeywords}from"./graph-related-keyword-query.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const result=queryGraphRelatedKeywords(db,{siteId:"site-a.example",query:"企業",depth:1});const retainedInventoryCount=Number(db.prepare("SELECT COUNT(*) AS count FROM imported_keywords WHERE site_id = ?").get("site-a.example").count);assert.equal(result.summary.inventory_row_count,retainedInventoryCount);assert.equal(result.summary.candidate_count,result.rows.length);assert.equal(result.summary.candidate_count,result.summary.lexical_supported_count+result.summary.semantic_path_candidate_count-result.summary.lexical_and_semantic_path_count);assert(result.summary.lexical_supported_count>0);assert(result.summary.semantic_path_candidate_count>0);assert.equal(result.summary.external_market_coverage_count,0);assert.equal(result.summary.auto_group_assignment_count,0);assert.equal(result.summary.auto_content_mutation_count,0);assert(result.query.semantic_queries[0].seed_senses.every(row=>row.sense_id.length===64&&row.synset&&row.definition));const candidate=result.rows.find(row=>row.match_state==="lexical_and_semantic_path");assert(candidate);assert(candidate.semantic_paths.some(row=>row.query_token==="企業"&&row.traversal_relation_type==="hypo"));assert(candidate.group_boundary.state);assert.equal(candidate.auto_group_assignment,false);assert(result.rows.every(row=>row.source_keyword_id&&row.evidence_digest.length===64&&!row.synonymy_inferred&&!row.search_demand_inferred&&!row.ranking_effect_inferred&&!row.auto_group_assignment&&!row.auto_content_mutation));const empty=queryGraphRelatedKeywords(db,{siteId:"site-a.example",query:""});assert.equal(empty.rows.length,0);assert.equal(empty.summary.auto_group_assignment_count??0,0);console.log(`graph related keyword query: OK (${retainedInventoryCount} retained rows, lexical/semantic paths separated, group boundaries fail closed)`)}finally{db.close()}
