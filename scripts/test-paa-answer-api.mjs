import assert from "node:assert/strict";
import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";
import {routeResearchApi} from "./keyword-dashboard-api.mjs";
import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";
const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const dashboard=projectDashboard(db),site=dashboard.sites[0],rows=dashboard.paa_answer_evidence.filter((row)=>row.site_id===site.site_id);assert.equal(rows.length,396);assert.equal(rows.filter((row)=>row.response_state==="resolved").length,12);assert.equal(rows.filter((row)=>row.response_state==="async_pending").length,384);assert.equal(rows.reduce((sum,row)=>sum+row.answer_count,0),12);assert.equal(rows.flatMap((row)=>row.answers).filter((answer)=>answer.table).length,1);assert(rows.every((row)=>row.evidence_digest.length===64&&!row.auto_mutation));const url=new URL(`http://localhost/api/v1/paa-answers?site_id=${site.site_id}&limit=100`),api=routeResearchApi(url.pathname,url,dashboard,db);assert.equal(api.status,200);assert.equal(api.body.summary.question_occurrence_count,396);assert.equal(api.body.summary.resolved_question_count,12);assert.equal(api.body.summary.async_pending_count,384);assert.equal(api.body.summary.source_domain_count,12);assert.equal(api.body.retry_mutation_supported,false);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"search_paa_answers",arguments:{site_id:site.site_id,state:"resolved",limit:100}}},dashboard);assert.equal(mcp.result.structuredContent.meta.total,12);assert.equal(mcp.result.structuredContent.summary.async_pending_count,0);console.log("PAA answer API/MCP: OK (396 questions, 12 resolved sources, 384 async pending, one table)")}finally{db.close()}
