import assert from "node:assert/strict";
import {resolveContentClaimEvidence} from "./content-claim-evidence.mjs";

const revision={sections:[{heading_candidate_id:"h1",paragraphs:[{claim_ids:["c1"]}]},{heading_candidate_id:"h2",paragraphs:[{claim_ids:["c2"]}]},{heading_candidate_id:"h3",paragraphs:[{claim_ids:["c3"]}]}],claims:[{claim_id:"intro",evidence_ids:[],verification_state:"not_applicable"},{claim_id:"c1",evidence_ids:["p1"],verification_state:"pending_primary_source"},{claim_id:"c2",evidence_ids:["page1"],verification_state:"pending_primary_source"},{claim_id:"c3",evidence_ids:["item1","missing"],verification_state:"pending_primary_source"}]};
const candidates=[{candidate_id:"h1",evidence_type:"serp_demand"},{candidate_id:"h2",evidence_type:"competitor_term"},{candidate_id:"h3",evidence_type:"serp_feature_item"}];
const result=resolveContentClaimEvidence(revision,candidates,{topics:[{proposal_id:"p1",display_topic:"質問",topic_kind:"paa",relation:"same_group",occurrence_count:2,task_count:1,evidence:[{occurrence_id:"o1"}]}],competitorPages:[{page_id:"page1",title:"競合",url:"https://example.com",domain:"example.com",status:"ok",snapshot_digest:"a".repeat(64)}],featureItems:[{feature_item_id:"item1",text:"候補",feature_type:"people_also_search",task_id:"t1",url:"https://source.example",evidence_digest:"b".repeat(64)}]});
assert.equal(result.references.length,4);assert.equal(result.oracle.resolved_evidence_reference_count,3);assert.equal(result.oracle.unresolved_evidence_reference_count,1);assert.equal(result.oracle.claim_with_resolved_evidence_count,3);assert.equal(result.oracle.all_references_resolved,false);assert.equal(result.oracle.fact_verification_state,"pending_primary_source");assert.equal(result.oracle.publication_state,"blocked");assert.equal(result.oracle.auto_approval,false);assert.equal(result.references.find((row)=>row.evidence_id==="page1").source.url,"https://example.com");assert.equal(result.references.find((row)=>row.evidence_id==="missing").resolution_state,"unresolved");assert.ok(result.references.every((row)=>row.reference_digest.length===64));assert.equal(result.oracle_digest.length,64);
console.log("content claim evidence: OK (typed source resolution, unresolved queue, fact/publication gate)");
