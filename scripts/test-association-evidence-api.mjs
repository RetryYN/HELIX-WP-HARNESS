import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/association-evidence?site_id=${site.site_id}&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db),s=api.body.summary;assert.equal(api.body.meta.total,1636);assert.equal(s.term_count,585);assert.equal(s.strong_count,336);assert.equal(s.moderate_count,395);assert.equal(s.weak_count,905);assert.equal(s.reciprocal_edge_count,1002);assert.equal(s.one_way_topk_count,634);assert.equal(s.complete_evidence_count,1589);assert.equal(s.capped_evidence_count,47);assert.equal(s.review_required_count,0);assert.equal(api.body.interpretation_policy,"observed_row_cooccurrence_not_semantic_equivalence");const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"audit_association_evidence",arguments:{site_id:site.site_id,reciprocal:"false",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,634);console.log("association evidence API/MCP: OK (1,636 edges, zero integrity anomalies, no synonym inference)")}finally{db.close()}
