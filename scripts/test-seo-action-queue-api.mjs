import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/action-queue?site_id=${site.site_id}&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db),s=api.body.summary;assert.equal(api.body.meta.total,142);assert.deepEqual(s.priority_counts,{P0:63,P1:14,P2:64,P3:1});assert.equal(s.type_counts.primary_source_verification,63);assert.equal(s.type_counts.content_gap_review,13);assert.equal(s.type_counts.content_topology_review,23);assert.equal(s.type_counts.internal_link_review,17);assert.equal(s.type_counts.serp_format_review,25);assert.equal(s.type_counts.rank_monitor_registration_review,1);assert(api.body.data.every((row)=>row.review_state==="review_required"&&row.execution_state==="not_executed"&&!row.auto_execution&&!row.rank_lift_inferred));assert.equal(api.body.auto_execution,false);assert.equal(api.body.rank_lift_inferred,false);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"review_seo_actions",arguments:{site_id:site.site_id,priority:"P0",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,63);console.log("SEO action queue API/MCP: OK (142 reviews, explicit workflow order, zero execution)")}finally{db.close()}
