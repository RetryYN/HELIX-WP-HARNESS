import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/simultaneous-rank-integrity?site_id=${site.site_id}&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db),s=api.body.summary;assert.equal(api.body.meta.total,339);assert.equal(s.same_group_count,85);assert.equal(s.cross_group_count,254);assert.equal(s.full_top10_overlap_count,2);assert.equal(s.boundary_connected_count,68);assert.equal(s.intent_only_count,96);assert.equal(s.url_only_count,175);assert.equal(s.review_required_count,0);assert.equal(api.body.merge_inferred_from_shared_urls,false);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"audit_simultaneous_rankings",arguments:{site_id:site.site_id,scope:"cross_group",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,254);console.log("simultaneous rank API/MCP: OK (339 relations, URL/rank reconstruction, zero integrity gaps)")}finally{db.close()}
