import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/observed-opportunity-content-coverage?site_id=${site.site_id}&review_action=monitor_existing_coverage&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db);assert.equal(api.status,200);assert.equal(api.body.summary.candidate_count,86);assert.equal(api.body.summary.observed_query_coverage_count,12);assert.equal(api.body.summary.strong_content_coverage_without_query_count,6);assert.equal(api.body.summary.partial_content_coverage_review_count,9);assert.equal(api.body.summary.new_article_review_count,59);assert.equal(api.body.meta.total,12);assert(api.body.data.every((row)=>row.query_matches.length&&!row.automatic_content_mutation&&!row.auto_publication&&!row.ranking_effect_inferred));const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"review_observed_opportunity_content_coverage",arguments:{site_id:site.site_id,review_action:"review_content_coverage_gap",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,9);assert.equal(mcp.result.structuredContent.provenance.external_acquisition_triggered,false);console.log("observed opportunity content coverage API/MCP: OK (12 query-covered, 6 strong, 9 gap reviews, 59 new)")}finally{db.close()}
