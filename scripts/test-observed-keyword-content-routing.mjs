import assert from "node:assert/strict";import {buildObservedKeywordContentRouting} from "./observed-keyword-content-routing.mjs";const portfolio={rows:[{target_domain:"target.test",keyword:"one",group_ids:["g1"],opportunity_score:1,observed_neighbor_domain_count:2,evidence_digest:"a",target_observation_state:"not_observed_for_target_within_retained_depth"},{target_domain:"target.test",keyword:"two",group_ids:["g2"],opportunity_score:.5,observed_neighbor_domain_count:1,evidence_digest:"b",target_observation_state:"not_observed_for_target_within_retained_depth"}]},groups=[{site_id:"s",id:"g1",main_keyword:"one",wp_article_id:1},{site_id:"s",id:"g2",main_keyword:"two",wp_article_id:null}],articles=[{site_id:"s",wp_article_id:1,title:"Existing",url:"https://target.test/a"}],readiness=[{group_id:"g1",publication_state:"blocked",blocker_codes:["claim"]},{group_id:"g2",publication_state:"blocked",blocker_codes:["citation"]}],result=buildObservedKeywordContentRouting(portfolio,groups,articles,readiness,{siteId:"s"});assert.equal(result.summary.candidate_count,2);assert.equal(result.summary.existing_article_expansion_review_count,1);assert.equal(result.summary.new_article_review_count,1);assert.equal(result.summary.connected_article_count,1);assert.equal(result.rows[0].action,"review_existing_article_expansion");assert.equal(result.rows[1].action,"review_new_article");assert(result.rows.every((row)=>!row.target_confirmed_unranked&&!row.automatic_content_mutation&&!row.auto_publication&&!row.ranking_effect_inferred));console.log("observed keyword content routing: OK (existing/new article reviews, blockers, no mutation/publication inference)");
