import assert from "node:assert/strict";import {buildPaidTestBudgetScenarios} from "./paid-test-budget-scenarios.mjs";
const candidates=[1,2,3,4].map((rank)=>({allocation_id:`a${rank}`,allocation_rank:rank,allocation_state:"selected_plan_only",unit_cost_usd:.2,priority_band:rank<3?"P0":"P2",capability:"keyword"})),allocation={rows:candidates,summary:{lifetime_cap_usd:1,committed_cost_usd:.1}},generationManifest={requests:[{maximum_cost_usd:null},{maximum_cost_usd:null}]},p=buildPaidTestBudgetScenarios({allocation,generationManifest,reservations:[{scenario_id:"base",generation_reservation_usd:0},{scenario_id:"reserve",generation_reservation_usd:.3}]});assert.equal(p.rows[0].acquisition_selected_count,4);assert.equal(p.rows[1].acquisition_selected_count,3);assert.equal(p.rows[1].acquisition_relinquished_count,1);assert.equal(p.rows[1].generation_capacity_request_count,null);assert.equal(p.rows[1].generation_capacity_state,"unknown_until_model_price_selected");assert(p.rows.every((row)=>row.within_lifetime_cap&&!row.reallocation_applied&&!row.external_acquisition_triggered&&!row.external_generation_triggered));assert.equal(p.summary.external_paid_execution_count,0);assert.equal(p.generation_capacity_claimed,false);assert.equal(p.portfolio_digest.length,64);console.log("paid test budget scenarios: OK (shared cap, acquisition opportunity cost, unknown generation capacity, zero execution)");
