import assert from "node:assert/strict";
import {buildContentTopologyOracle} from "./content-topology-oracle.mjs";

const depth=(left,right,state,robust=false)=>({left_task_id:`${left}t`,right_task_id:`${right}t`,left_group_id:left,right_group_id:right,left_keyword:left,right_keyword:right,stability_state:state,robust_merge:robust,stability_digest:"d".repeat(64)}),groups=[{id:"a",main_keyword:"A",state:"未施策"},{id:"b",main_keyword:"B",state:"未施策"},{id:"c",main_keyword:"C",state:"未施策"}],structures=[{group_id:"a",composition:{title_candidate_id:"ta",selected_heading_ids:["h1"],review_state:"ready",composition_digest:"a".repeat(64)}},{group_id:"b",composition:{title_candidate_id:"tb",selected_heading_ids:["h2","h3"],review_state:"ready",composition_digest:"b".repeat(64)}}],candidates=[{candidate_id:"ta",text:"A title"},{candidate_id:"tb",text:"B title"}];const result=buildContentTopologyOracle([depth("a","b","stable_merge",true),depth("a","c","threshold_flip"),depth("b","c","stable_separate")],groups,structures,candidates);assert.deepEqual(result.rows.map((row)=>row.topology_decision),["consolidation_review","depth_sensitive_hold","retain_separate"]);const merge=result.rows[0];assert.equal(merge.selected_title_count,2);assert.equal(merge.selected_heading_count,3);assert.equal(merge.survivor_selection_state,"unresolved_not_auto_selected");assert.equal(result.summary.group_pair_count,3);assert.equal(result.summary.review_count,2);assert.ok(result.rows.every((row)=>row.topology_digest.length===64&&!row.auto_mutation));console.log("content topology oracle: OK (group-pair collapse, consolidation/hold/separate actions, no auto mutation)");
