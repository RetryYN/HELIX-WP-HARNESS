import assert from"node:assert/strict";import{openDashboardDb,projectDashboard}from"./keyword-dashboard-db.mjs";import{routeResearchApi}from"./keyword-dashboard-api.mjs";import{handleMcpMessage}from"./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],oracle=site.new_article_citation_suitability_gate,url=new URL(`/api/v1/new-article-citation-suitability?site_id=${site.site_id}&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db);assert.equal(api.status,200);for(const field of["brief_count","claim_count","recommendation_count","unique_url_count","primary_source_proven_url_count","primary_source_unproven_url_count","requirement_satisfied_claim_count","requirement_gap_claim_count"])assert.equal(api.body.summary[field],oracle.summary[field]);assert.deepEqual(api.body.summary.source_class_counts,oracle.summary.source_class_counts);assert(api.body.data.every(row=>!row.requirement_satisfied&&!row.auto_approval&&!row.auto_publication));const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"audit_new_article_citation_suitability",arguments:{site_id:site.site_id,suitability_state:"primary_source_proof_missing",limit:100}}},data);assert(mcp.result.structuredContent.meta.total>0);assert.equal(mcp.result.structuredContent.provenance.external_acquisition_triggered,false);console.log(`new article citation suitability API/MCP: OK (${oracle.summary.recommendation_count} candidates, ${oracle.summary.unique_url_count} URLs, ${oracle.summary.requirement_satisfied_claim_count} source requirements proven)`)}finally{db.close()}
