import assert from "node:assert/strict";
import {buildEvidenceSafeManualRevisionPackets} from "./evidence-safe-manual-revision-packets.mjs";

const proposal={revision_proposal_id:"safe-revision:g:section:1",claim_id:"g:section:1",group_id:"g",proposed_heading:"安全な見出し",proposed_body_scope:"確認できる範囲と確認先を説明する。",source_evidence_ids:["e1"],source_citation_ids:[],proposal_digest:"a".repeat(64),decision_digests:["b".repeat(64)],progress_digest:"c".repeat(64),editorial_progress_state:"approved_for_manual_revision",manual_revision_eligible:true,publication_blocker_codes:["source_verification_pending","citation_approval_pending"]},source={revision:1,title:"元タイトル",sections:[{section_id:"g:section:1",heading_candidate_id:"h1",level:2,heading:"危険な断定見出し",parent_candidate_id:null,paragraphs:[{paragraph_id:"g:section:1:p1",text:"確認できない事実を断定する。",claim_ids:["g:section:1"]}],evidence_ids:["e1"]}],claims:[{claim_id:"g:intro",text:"導入",evidence_ids:[],citation_ids:[]},{claim_id:"g:section:1",text:"確認できない事実を断定する。",evidence_ids:["e1"],citation_ids:[]}],evidence_ids:["e1"],citation_ids:[],text:"before",html:"<article>before</article>",revision_digest:"d".repeat(64)},structures=[{group_id:"g",draft_package:{draft_revision:source}}],approved=buildEvidenceSafeManualRevisionPackets({rows:[proposal],proposal_set_digest:"e".repeat(64)},structures);assert.equal(approved.summary.packet_count,1);assert.equal(approved.summary.eligible_proposal_count,1);assert.equal(approved.summary.artifact_applied_count,0);assert.equal(approved.packets[0].after.sections[0].heading,"安全な見出し");assert.equal(approved.packets[0].after.claims[1].text,proposal.proposed_body_scope);assert.deepEqual(approved.packets[0].after.evidence_ids,source.evidence_ids);assert.equal(approved.packets[0].evidence_and_citation_lineage_intact,true);assert.equal(approved.packets[0].artifact_applied,false);assert.equal(approved.packets[0].auto_publication,false);const pending=buildEvidenceSafeManualRevisionPackets({rows:[{...proposal,manual_revision_eligible:false,editorial_progress_state:"unreviewed"}],proposal_set_digest:"e".repeat(64)},structures);assert.equal(pending.summary.packet_count,0);assert.equal(pending.summary.blocked_proposal_count,1);assert(pending.blocked_proposals[0].blocker_codes.includes("editor_decision_pending"));console.log("evidence-safe manual revision packets: OK (approved-only diff, intact lineage, pending empty state, no apply or publication)");
