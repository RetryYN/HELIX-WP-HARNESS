import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/title-repairs?site_id=${site.site_id}&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db),s=api.body.summary;assert.equal(api.status,200);assert.equal(api.body.meta.total,63);assert.equal(s.ready_for_editor_review_count,62);assert.equal(s.needs_review_count,1);assert.equal(s.within_observed_iqr_count,62);assert.equal(s.topic_evidence_connected_count,57);assert.equal(s.auto_approved_count,0);assert(api.body.data.every((row)=>!row.auto_approval&&!row.ranking_effect_inferred&&!row.copying_competitor_title));assert.equal(api.body.auto_approval,false);assert.equal(api.body.ranking_effect_inferred,false);assert.equal(api.body.copying_competitor_title,false);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"review_title_repairs",arguments:{site_id:site.site_id,state:"ready_for_editor_review",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,62);console.log("title repair API/MCP: OK (63 repairs, 62 observed-IQR editor reviews, no copy/approval/rank inference)")}finally{db.close()}
