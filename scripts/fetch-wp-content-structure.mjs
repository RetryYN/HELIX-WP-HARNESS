import {mkdir,writeFile} from "node:fs/promises";import path from "node:path";import {createHash} from "node:crypto";import {extractWpContentStructure} from "./wp-content-structure.mjs";
const siteUrl=process.env.WP_SITE_URL??"https://it-shukatu-college.com/",siteId=process.env.WP_SITE_ID??"it-shukatu.com",output=path.resolve(process.argv[2]??".helix/evidence/wp-content-structure/manifest.json"),articles=[];for(let page=1;;page+=1){const endpoint=new URL("wp-json/wp/v2/posts",siteUrl);endpoint.search=new URLSearchParams({per_page:"100",page:String(page),orderby:"id",order:"asc",_fields:"id,link,modified,content"});const response=await fetch(endpoint);if(response.status===400&&page>1)break;if(!response.ok)throw new Error(`WP posts: HTTP ${response.status}`);for(const post of await response.json())articles.push(extractWpContentStructure({siteId,wpArticleId:post.id,url:post.link,modified:post.modified,content:post.content.rendered}));if(page>=Number(response.headers.get("x-wp-totalpages")??1))break}const base={schema_version:"wp-content-structure-manifest.v1",generated_at:new Date().toISOString(),site_id:siteId,site_url:siteUrl,content_retained:false,articles},manifest={...base,manifest_digest:createHash("sha256").update(JSON.stringify(base)).digest("hex")};await mkdir(path.dirname(output),{recursive:true});await writeFile(output,`${JSON.stringify(manifest)}\n`);console.log(JSON.stringify({output,articles:articles.length,links:articles.reduce((sum,row)=>sum+row.links.length,0),internal_links:articles.reduce((sum,row)=>sum+row.links.filter((link)=>link.is_internal).length,0),images:articles.reduce((sum,row)=>sum+row.images.length,0),content_retained:false},null,2));
