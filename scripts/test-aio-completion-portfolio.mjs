import assert from "node:assert/strict";import {buildAioCompletionPortfolio} from "./aio-completion-portfolio.mjs";const rows=[{task_id:"a",response_state:"async_pending"},{task_id:"b",response_state:"resolved"}],scopes=[{task_id:"a",group_id:"g",site_id:"s",keyword:"IT",observed_at:"2026-08-23 00:00:00 +00:00"},{task_id:"b",group_id:"g",site_id:"s",keyword:"就活",observed_at:"2026-08-23 00:00:00 +00:00"}],p=buildAioCompletionPortfolio(rows,scopes,{asOf:new Date("2026-08-29T00:00:00Z")});assert.equal(p.candidates.length,1);assert.equal(p.summary.async_pending_count,1);assert.equal(p.summary.eligible_task_get_count,1);assert.equal(p.summary.avoided_repost_count,1);assert.equal(p.summary.provider_charge_usd,0);assert.equal(p.candidates[0].request.method,"GET");assert.match(p.candidates[0].request.endpoint,/task_get\/advanced\/a$/);assert.equal(p.candidates[0].retention_deadline,"2026-09-22T00:00:00.000Z");const expired=buildAioCompletionPortfolio(rows,scopes,{asOf:new Date("2026-09-23T00:00:00Z")});assert.equal(expired.summary.eligible_task_get_count,0);assert.equal(expired.summary.expired_or_unverified_count,1);assert.equal(expired.candidates[0].request.method,null);assert(p.candidates.every((row)=>!row.external_retrieval_triggered&&!row.auto_polling&&!row.auto_merge));console.log("AIO completion portfolio: OK (retention eligibility, pending-only free GET, no repost/poll)");
