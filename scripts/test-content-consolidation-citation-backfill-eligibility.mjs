import assert from "node:assert/strict";
import {auditConsolidationCitationBackfillEligibility} from "./content-consolidation-citation-backfill-eligibility.mjs";

const candidate={left_group_id:"a",right_group_id:"b",backfill_digest:"d".repeat(64),target_source_group_id:"b",target_source_claim_id:"b:c",merged_claim_id:"m",citation_source_group_ids:["x","y"]},result=auditConsolidationCitationBackfillEligibility([candidate],{intentPairs:[{left_group_id:"y",right_group_id:"b",intent_similarity_score:.6,decision:"related_intent"}],boundaries:[{left_group_id:"x",right_group_id:"b",url_overlap_ratio:.3,decision:"internal_link_boundary_review"}],depthStability:[{left_group_id:"x",right_group_id:"b",stability_state:"threshold_flip",robust_merge:false}]});
assert.equal(result.rows.length,1);assert.equal(result.rows[0].eligibility_state,"adjacent_evidence_review");assert.equal(result.rows[0].assessed_source_group_count,2);assert.deepEqual(result.rows[0].unassessed_source_group_ids,[]);assert.equal(result.rows[0].threshold_flip_source_group_count,1);assert.ok(result.rows[0].reason_codes.includes("cross_group_merge_consensus_absent"));assert.ok(result.rows[0].reason_codes.includes("cross_group_depth_threshold_flip"));assert.equal(result.rows[0].application_state,"blocked_pending_editor_and_primary_source");assert.equal(result.rows[0].auto_approval,false);assert.equal(result.rows[0].eligibility_digest.length,64);assert.deepEqual(result.summary,{candidate_count:1,robust_count:0,adjacent_review_count:1,unverified_count:0,threshold_flip_count:1,unassessed_source_group_candidate_count:0,blocked_count:1,auto_approval:false});
console.log("content consolidation citation backfill eligibility: OK (intent/boundary/depth evidence, blocked review)");
