import assert from "node:assert/strict";import {buildClaimDiscoveryPortfolio} from "./claim-discovery-portfolio.mjs";const row=(id,p,r)=>({claim_id:id,group_id:"g",main_keyword:"IT",topic:id,priority_band:p,source_requirement:r,discovery_queries:[`${id} 公式`,`${id} site:go.jp`,`${id} 一次情報`],citation_candidate_count:0}),o=buildClaimDiscoveryPortfolio({rows:[row("a","P0","primary_official_source_required"),row("b","P1","authoritative_definition_or_entity_source_required"),row("c","P2","independent_corroboration_required")]},2);assert.equal(o.candidates[0].selected_query,"a 公式");assert.equal(o.candidates[1].selected_query,"b site:go.jp");assert.equal(o.batches.length,2);assert.equal(o.summary.avoided_initial_query_count,6);assert(o.candidates.every((x)=>!x.external_discovery_executed&&!x.auto_approval));console.log("claim discovery portfolio: OK (requirement-specific query selection, batching, no execution/approval)");
