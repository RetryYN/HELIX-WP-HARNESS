import assert from "node:assert/strict";
import {buildClaimCitationRecommendations} from "./content-claim-citations.mjs";

const revision={sections:[{heading_candidate_id:"h1",heading:"IT企業の選び方",paragraphs:[{claim_ids:["c1"]}]},{heading_candidate_id:"h2",heading:"面接準備",paragraphs:[{claim_ids:["c2"]}]}],claims:[{claim_id:"intro",verification_state:"not_applicable"},{claim_id:"c1",verification_state:"pending_primary_source"},{claim_id:"c2",verification_state:"pending_primary_source"}]};
const citations=[{citation_id:"a1",canonical_url:"https://example.com/it?utm_source=x",url:"https://example.com/it",domain:"example.com",title:"IT企業の選び方",text:"企業比較",source:"資料",source_keyword:"IT企業 就活",organic_url_rank:2,organic_domain_rank:2},{citation_id:"a2",url:"https://example.com/it",domain:"example.com",title:"IT企業の選び方",text:"企業比較",source:"資料",source_keyword:"IT 就活",organic_url_rank:1,organic_domain_rank:1},{citation_id:"b1",url:"https://example.net/interview",domain:"example.net",title:"面接の準備",text:"質問への備え",source:"面接資料",source_keyword:"面接 準備",organic_url_rank:null,organic_domain_rank:4}];
const result=buildClaimCitationRecommendations(revision,citations);
assert.equal(result.summary.applicable_claim_count,2);assert.equal(result.summary.claim_with_candidate_count,2);assert.equal(result.summary.recommendation_count,2);assert.equal(result.summary.approved_count,0);assert.equal(result.summary.auto_approval,false);assert.equal(result.recommendations.find((row)=>row.claim_id==="c1").occurrence_count,2);assert.equal(result.recommendations.find((row)=>row.claim_id==="c1").url,"https://example.com/it");assert.ok(result.recommendations.every((row)=>row.review_state==="needs_review"&&row.approval_state==="unreviewed"&&!row.auto_approval&&row.recommendation_digest.length===64));assert.equal(result.summary_digest.length,64);
console.log("content claim citations: OK (URL dedupe, lexical/rank scoring, review queue, no auto-approval)");
