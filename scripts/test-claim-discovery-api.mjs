import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/claim-discovery-portfolio?site_id=${site.site_id}&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db),s=api.body.summary;assert.equal(api.status,200);assert.equal(api.body.meta.total,510);assert.equal(s.selected_query_count,510);assert.equal(s.avoided_initial_query_count,1020);assert.equal(s.batch_count,6);assert.equal(s.retained_citation_candidate_count,107);assert.equal(s.missing_citation_candidate_count,403);assert.equal(s.external_discovery_executed_count,0);assert.equal(api.body.estimated_cost_usd,null);assert(api.body.data.every((row)=>!row.external_discovery_executed&&!row.auto_approval));const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"plan_claim_discovery",arguments:{site_id:site.site_id,view:"batches",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,6);console.log("claim discovery API/MCP: OK (510 selected queries, 1,020 alternatives deferred, 6 batches, zero execution)")}finally{db.close()}
