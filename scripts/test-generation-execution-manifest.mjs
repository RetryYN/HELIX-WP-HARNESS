import "./test-generation-manifest-source-input.mjs";
import assert from "node:assert/strict";
import {buildGenerationExecutionManifest} from "./generation-execution-manifest.mjs";
const structures=[{group_id:"g",draft_package:{package_version:"draft.v1",input_digest:"a".repeat(64),package_digest:"b".repeat(64),input:{instruction:"根拠のみ",title_candidate_id:"t",headings:[{candidate_id:"h"}],topic_proposal_ids:["p"],citation_candidates:[{citation_id:"c"}]}}}],allocation={summary:{lifetime_cap_usd:5,committed_cost_usd:.3014,selected_plan_cost_usd:4.69815,remaining_after_selected_plan_usd:.00045}},blocked=buildGenerationExecutionManifest({siteId:"s",structures,lifetimeAllocation:allocation});assert.equal(blocked.summary.request_count,1);assert.equal(blocked.summary.technical_ready_count,0);assert.equal(blocked.summary.unallocated_lifetime_budget_usd,.00045);assert.equal(blocked.requests[0].maximum_cost_usd,null);assert(blocked.requests[0].blockers.includes("model_identity_required"));assert(blocked.requests[0].blockers.includes("maximum_cost_unknown"));assert.equal(blocked.requests[0].execution_authorized,false);assert.equal(blocked.summary.external_generation_triggered_count,0);assert.equal(blocked.requests[0].prompt_contract.natural_language_prompt_recorded,true);const priced=buildGenerationExecutionManifest({siteId:"s",structures,lifetimeAllocation:allocation,modelSelection:{provider:"test",model:"test-model",input_usd_per_million_tokens:1,output_usd_per_million_tokens:2,price_observed_at:"2026-08-29"}});assert(priced.requests[0].maximum_cost_usd>.0002);assert(priced.requests[0].blockers.includes("outside_unallocated_lifetime_budget"));assert.equal(priced.requests[0].technical_ready,false);assert.equal(priced.manifest_digest.length,64);console.log("generation execution manifest: OK (prompt/output contract, model-price fail closed, global $5 budget connection, zero execution)");
