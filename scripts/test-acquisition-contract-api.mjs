import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],api=routeResearchApi("/api/v1/acquisition-contract-fulfillment",new URL(`http://localhost/api/v1/acquisition-contract-fulfillment?site_id=${site.site_id}&state=expansion_required&limit=100`),data,db),s=api.body.summary;assert.equal(api.status,200);assert.equal(api.body.meta.total,98);assert.equal(s.task_count,100);assert.equal(s.expansion_required_count,98);assert.equal(s.aio_async_load_not_requested_count,51);assert.equal(s.paa_click_depth_not_requested_count,96);assert.equal(s.recorded_cost_usd,.2);assert.equal(s.auto_retrieval_count,0);assert(api.body.data.every((row)=>row.fulfillment_state==="expansion_required"&&!row.auto_retrieval));const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"audit_acquisition_contracts",arguments:{site_id:site.site_id,state:"expansion_required",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,98);console.log("acquisition contract API/MCP: OK (100 tasks, 98 expansion reviews, zero retrieval)")}finally{db.close()}
