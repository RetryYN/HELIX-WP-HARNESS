import assert from "node:assert/strict";
import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";
import {routeResearchApi} from "./keyword-dashboard-api.mjs";
import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";
const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/keyword-lineage?site_id=${encodeURIComponent(site.site_id)}&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db),summary=api.body.summary;assert.equal(api.status,200);assert.equal(api.body.meta.total,10694);assert.equal(summary.source_identity_count,10694);assert.equal(summary.hierarchy_connected_count,10694);assert.equal(summary.acquired_unique_group_count,100);assert.equal(summary.acquired_multi_group_review_count,0);assert.equal(summary.unacquired_recommendation_connected_count,733);assert.equal(summary.inventory_only_count,9861);assert.equal(summary.lineage_anomaly_count,0);assert.equal(summary.normalized_duplicate_row_count,149);assert.equal(api.body.source_rows_losslessly_retained,true);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"audit_keyword_lineage",arguments:{site_id:site.site_id,duplicates:"true",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,149);console.log("keyword lineage API/MCP: OK (10,694 lossless source rows, 100 acquired group links, zero anomalies)")}finally{db.close()}
