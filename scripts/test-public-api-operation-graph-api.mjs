import assert from "node:assert/strict";import {routeResearchApi,researchOpenApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";
const data={sites:[],groups:[]},request=(query="")=>{const url=new URL(`http://localhost/api/v1/operation-graph?${query}`);return routeResearchApi(url.pathname,url,data)},all=request("limit=100");assert.equal(researchOpenApi.info.version,"2.118.0");assert.equal(all.status,200);assert.equal(all.body.meta.total,24);assert.equal(all.body.summary.operation_count,24);assert.equal(all.body.summary.credit_contract_counts.unclassified,0);assert.equal(all.body.external_request_executed,false);assert.equal(all.body.provenance.external_acquisition_triggered,false);const dynamic=request("credit=dynamic&limit=100");assert.equal(dynamic.body.meta.total,3);assert(dynamic.body.data.every((row)=>row.credit_contract_kind==="dynamic"));const register=request("role=register&limit=100");assert.equal(register.body.meta.total,2);assert(register.body.data.every((row)=>row.async_polling_documented));const query=request("q=seoDifficulty&limit=100");assert(query.body.data.some((row)=>row.operation_id==="SearchVolumeHistoryController_register"));const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"inspect_public_api_operation_graph",arguments:{credit:"zero",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,9);const tools=handleMcpMessage({jsonrpc:"2.0",id:2,method:"tools/list"},data).result.tools;assert.equal(tools.length,108);console.log("public API operation graph API/MCP: OK (API 2.118.0, 24 contracts, 108 tools, external requests 0)");
