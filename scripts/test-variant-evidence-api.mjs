import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/variant-evidence?site_id=${site.site_id}&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db),s=api.body.summary;assert.equal(api.body.meta.total,921);assert.equal(s.variant_occurrence_count,2075);assert.equal(s.variant_pair_count,1492);assert.equal(s.largest_cluster_size,7);assert.equal(s.verified_count,921);assert.equal(s.review_required_count,0);assert.equal(api.body.semantic_synonymy_inferred,false);assert.equal(api.body.external_dictionary_acquired,false);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"audit_variant_evidence",arguments:{site_id:site.site_id,state:"verified",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,921);console.log("variant evidence API/MCP: OK (921 clusters, 2,075 forms, zero synonym inference)")}finally{db.close()}
