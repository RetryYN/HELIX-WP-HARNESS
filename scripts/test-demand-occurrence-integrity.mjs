import assert from "node:assert/strict";import {readFileSync} from "node:fs";import {buildDemandOccurrenceIntegrity} from "./demand-occurrence-integrity.mjs";const occurrence={occurrence_id:"o",demand_type:"paa",normalized_value:"x",value:"X",task_id:"t",group_id:"g",source_keyword:"k",observed_at:"2026-01-01T00:00:00Z",snapshot_path:"/tmp/snapshot.json",snapshot_digest:"a".repeat(64),recursion_depth:1},aggregate={demand_type:"paa",normalized_value:"x",representative_value:"X",occurrence_count:1,task_count:1,group_count:1,task_ids:["t"],group_ids:["g"],source_keywords:["k"],first_observed_at:occurrence.observed_at,last_observed_at:occurrence.observed_at,best_serp_rank:2,importance_score:10},feature={feature_id:"f",task_id:"t",feature_type:"people_also_ask",payload:{items:[{title:"X",expanded_element:[{asynchronous_ai_overview:true,items:null,references:null}]}]}};const result=buildDemandOccurrenceIntegrity([aggregate],[occurrence],[feature]);assert.equal(result.summary.review_required_count,0);assert.equal(result.rows[0].appearance_history_state,"single_day_snapshot_only");assert.equal(result.rows[0].absolute_search_volume_inferred,false);assert.equal(result.summary.payload_evidence_evaluated,true);assert.equal(result.summary.feature_payload_linked_occurrence_count,1);assert.equal(result.summary.feature_payload_unlinked_occurrence_count,0);assert.equal(result.summary.paa_answer_state_counts.async_pending,1);assert.equal(result.summary.paa_answer_not_returned_occurrence_count,1);assert.equal(result.rows[0].retention_evidence.feature_payload_link_state,"all_occurrences_linked");const html=readFileSync("docs/prototypes/wp-ops-dashboard/index.html","utf8"),ui=readFileSync("docs/prototypes/wp-ops-dashboard/demand-payload-retention.js","utf8");assert.match(html,/id="demand-payload-retention"/);assert.match(html,/demand-payload-retention\.js/);assert.match(ui,/feature_payload_linked_occurrence_count/);assert.match(ui,/provider未返却/);console.log("demand occurrence integrity: OK (aggregate lineage, feature payload retention, provider answer boundary)");
