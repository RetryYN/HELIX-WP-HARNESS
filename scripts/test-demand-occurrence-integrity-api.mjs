import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/demand-occurrence-integrity?site_id=${site.site_id}&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db),s=api.body.summary;assert.equal(api.body.meta.total,714);assert.equal(s.occurrence_count,1188);assert.equal(s.paa_demand_count,221);assert.equal(s.related_search_demand_count,493);assert.equal(s.cross_group_repeated_count,86);assert.equal(s.within_group_repeated_count,105);assert.equal(s.single_task_count,523);assert.equal(s.multi_day_history_count,0);assert.equal(s.single_day_snapshot_count,714);assert.equal(s.review_required_count,0);assert.equal(s.orphan_occurrence_count,0);assert.equal(api.body.absolute_search_volume_inferred,false);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"audit_demand_occurrences",arguments:{site_id:site.site_id,scope:"cross_group_repeated",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,86);console.log("demand occurrence API/MCP: OK (714 demands, 1,188 occurrences, zero integrity gaps)")}finally{db.close()}
