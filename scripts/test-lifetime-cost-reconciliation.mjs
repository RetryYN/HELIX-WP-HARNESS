import assert from"node:assert/strict";import{openDashboardDb,projectDashboard}from"./keyword-dashboard-db.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const site=projectDashboard(db).sites[0],state=site.acquisition_lifetime_state,ledger=site.provider_cost_ledger,reconciliation=ledger.reconciliation,allocation=site.acquisition_lifetime_allocation;assert.equal(state.legacy_committed_cost_usd,.2);assert.equal(state.run_committed_cost_usd,.1014);assert.equal(state.committed_cost_usd,.3014);assert.equal(state.remaining_budget_usd,4.6986);assert.equal(ledger.entry_count,197);assert.equal(ledger.total_cost_usd,.3014);assert.equal(reconciliation.state,"reconciled");assert.equal(reconciliation.difference_usd,0);assert.equal(reconciliation.analysis_retained_entry_count,100);assert.equal(reconciliation.replaced_legacy_entry_count,0);assert.equal(reconciliation.retained_remediation_result_cost_entry_count,96);assert.equal(reconciliation.non_result_cost_entry_count,1);assert.equal(reconciliation.cost_history_retained_entry_count,197);assert(ledger.entries.every(row=>row.source_digest?.length===64));assert.equal(allocation.summary.previous_attempt_review_count,98);assert.equal(allocation.rows.filter(row=>row.previous_attempt_observed&&row.allocation_state==="selected_plan_only").length,0);assert(allocation.rows.filter(row=>row.previous_attempt_observed).every(row=>row.allocation_reason==="previous_attempt_requires_review"));const budget=site.acquisition_lifetime_approval_manifest.lifetime_budget;assert(budget.projected_maximum<=budget.cap);assert.equal(Number((budget.cap-budget.projected_maximum).toFixed(8)),budget.remaining_after_projected_maximum);assert.equal(site.acquisition_lifetime_approval_manifest.execution_authorized,false);console.log("lifetime cost reconciliation: OK ($0.3014 committed across 197 lossless entries; 98 prior attempts excluded; $5 cap preserved)")}finally{db.close()}
