import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/semantic-candidate-reviews?site_id=${site.site_id}&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db),s=api.body.summary;assert.equal(api.body.meta.total,168);assert.equal(s.possible_compound_count,2);assert.equal(s.contextual_relation_count,23);assert.equal(s.distinct_context_count,41);assert.equal(s.insufficient_context_count,102);assert.equal(s.shared_url_occurrence_count,330);assert.equal(s.editor_decided_count,0);assert.equal(api.body.semantic_equivalence_inferred,false);assert.equal(api.body.editor_decision_required,true);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"review_semantic_candidates",arguments:{site_id:site.site_id,state:"contextual_relation_review",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,23);console.log("semantic candidate API/MCP: OK (168 relation candidates, editor review required, no synonym assertion)")}finally{db.close()}
