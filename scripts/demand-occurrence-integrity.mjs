import {createHash} from "node:crypto";const digest=(value)=>createHash("sha256").update(JSON.stringify(value)).digest("hex");
export function buildDemandOccurrenceIntegrity(aggregates,occurrences){const byKey=Map.groupBy(occurrences,(row)=>`${row.demand_type}\0${row.normalized_value}`),rows=aggregates.map((aggregate)=>{const evidence=byKey.get(`${aggregate.demand_type}\0${aggregate.normalized_value}`)??[],taskIds=[...new Set(evidence.map((row)=>row.task_id))].sort(),groupIds=[...new Set(evidence.map((row)=>row.group_id))].sort(),sourceKeywords=[...new Set(evidence.map((row)=>row.source_keyword))].sort(),observed=evidence.map((row)=>row.observed_at).sort(),anomalies=[];if(evidence.length!==aggregate.occurrence_count)anomalies.push("occurrence_count_mismatch");if(taskIds.length!==aggregate.task_count)anomalies.push("task_count_mismatch");if(groupIds.length!==aggregate.group_count)anomalies.push("group_count_mismatch");if(JSON.stringify(taskIds)!==JSON.stringify([...aggregate.task_ids].sort()))anomalies.push("task_identity_mismatch");if(JSON.stringify(groupIds)!==JSON.stringify([...aggregate.group_ids].sort()))anomalies.push("group_identity_mismatch");if(JSON.stringify(sourceKeywords)!==JSON.stringify([...aggregate.source_keywords].sort()))anomalies.push("source_keyword_mismatch");if(observed[0]!==aggregate.first_observed_at||observed.at(-1)!==aggregate.last_observed_at)anomalies.push("observation_window_mismatch");if(evidence.some((row)=>!row.snapshot_digest||row.snapshot_digest.length!==64))anomalies.push("snapshot_digest_missing");const scope=groupIds.length>1?"cross_group_repeated":taskIds.length>1?"within_group_repeated":"single_task_observation",days=new Set(observed.map((value)=>value.slice(0,10)));const base={demand_type:aggregate.demand_type,normalized_value:aggregate.normalized_value,representative_value:aggregate.representative_value,occurrence_count:evidence.length,task_count:taskIds.length,group_count:groupIds.length,source_keyword_count:sourceKeywords.length,best_serp_rank:aggregate.best_serp_rank,max_recursion_depth:Math.max(0,...evidence.map((row)=>row.recursion_depth)),importance_score:aggregate.importance_score,scope_state:scope,appearance_history_state:days.size>1?"multi_day_observed":"single_day_snapshot_only",observed_day_count:days.size,first_observed_at:observed[0]??null,last_observed_at:observed.at(-1)??null,occurrence_ids:evidence.map((row)=>row.occurrence_id),task_ids:taskIds,group_ids:groupIds,integrity_state:anomalies.length?"review_required":"verified",anomalies,absolute_search_volume_inferred:false,auto_mutation:false,policy:"demand-occurrence-integrity.v1"};return{...base,evidence_digest:digest(base)}});const orphanOccurrenceCount=[...byKey].filter(([key])=>!aggregates.some((row)=>`${row.demand_type}\0${row.normalized_value}`===key)).reduce((sum,[,items])=>sum+items.length,0);return{rows,summary:{demand_count:rows.length,occurrence_count:occurrences.length,paa_demand_count:rows.filter((row)=>row.demand_type==="paa").length,related_search_demand_count:rows.filter((row)=>row.demand_type==="related_search").length,cross_group_repeated_count:rows.filter((row)=>row.scope_state==="cross_group_repeated").length,within_group_repeated_count:rows.filter((row)=>row.scope_state==="within_group_repeated").length,single_task_count:rows.filter((row)=>row.scope_state==="single_task_observation").length,multi_day_history_count:rows.filter((row)=>row.appearance_history_state==="multi_day_observed").length,single_day_snapshot_count:rows.filter((row)=>row.appearance_history_state==="single_day_snapshot_only").length,review_required_count:rows.filter((row)=>row.integrity_state==="review_required").length,orphan_occurrence_count:orphanOccurrenceCount},importance_policy:"relative_within_retained_corpus_not_absolute_demand",appearance_policy:"snapshot_observation_not_continuous_history",auto_mutation:false,policy:"demand-occurrence-integrity.v1"}}
