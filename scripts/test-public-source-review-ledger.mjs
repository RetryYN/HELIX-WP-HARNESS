import assert from "node:assert/strict";
import {attachPublicSourceReviewProgress,buildPublicSourceReviewPacket,validatePublicSourceReviewDecisions} from "./public-source-review-ledger.mjs";
const evidence={evidence_set_digest:"e".repeat(64),rows:[{claim_id:"g:section:1",group_id:"g",priority_band:"P0",source_requirement:"primary_official_source_required",url:"https://example.test/source",source_class:"official_entity_page",publisher:"Example",title:"Source",observed_facts:["fact"],source_text_digest:"s".repeat(64),evidence_digest:"a".repeat(64),direct_support_state:"directly_supported",requirement_satisfied:true},{claim_id:"g:section:2",group_id:"g",priority_band:"P1",evidence_digest:"b".repeat(64),direct_support_state:"contextual_only",requirement_satisfied:false}]},packet=buildPublicSourceReviewPacket(evidence);assert.equal(packet.items.length,1);assert.equal(packet.summary.publication_unblocked_count,0);
const item=packet.items[0],reviewer="c".repeat(64),base={schema_version:"public-source-review-decisions.v1",packet_digest:packet.packet_digest,reviewer_digest:reviewer,decisions:[{review_id:item.review_id,review_digest:item.review_digest,editorial_state:"approved_for_claim_use",source_identity_verified:true,source_requirement_verified:true,claim_direct_support_verified:true,reviewed_at:"2026-08-31T01:00:00+09:00",notes:"fixture"}]},validated=validatePublicSourceReviewDecisions(packet,base),progress=attachPublicSourceReviewProgress(packet,[validated],validated.decisions.map((row)=>({...row,packet_digest:packet.packet_digest})));assert.equal(progress.summary.approved_for_claim_use_count,1);assert.equal(progress.summary.publication_unblocked_count,0);assert(progress.items[0].source_verified&&progress.items[0].citation_approved&&!progress.items[0].auto_apply&&!progress.items[0].auto_publication);assert.throws(()=>validatePublicSourceReviewDecisions(packet,{...base,decisions:[{...base.decisions[0],source_requirement_verified:false}]}),/prerequisites/);console.log("public source review ledger: OK (eligible-only, digest-bound, approval prerequisites, no auto apply/publication)");
