import assert from "node:assert/strict";
import {readFileSync} from "node:fs";
import {buildEditorialDecisionExport,editorialReviewCriteria} from "../docs/prototypes/wp-ops-dashboard/editorial-review-export.mjs";
import {buildContentEditorialReviewPacket,validateEditorialReviewDecisions} from "./content-editorial-review-packet.mjs";
const app=readFileSync("docs/prototypes/wp-ops-dashboard/app.js","utf8"),html=readFileSync("docs/prototypes/wp-ops-dashboard/index.html","utf8");assert.match(app,/buildEditorialDecisionExport/);assert.match(app,/editorial-semantic-input/);assert.match(app,/importerで検証後に明示commit/);assert.match(html,/editorial-reviewer-id/);assert.match(html,/editorial-review-export/);
const delta={explanation_id:"a",explanation_digest:"a".repeat(64),group_id:"g",main_keyword:"IT就活",content_type:"heading",current_candidate_id:"c",recommended_candidate_id:"r",current_text:"現行",recommended_text:"推奨",comparison_state:"net_evidence_gain",dominant_gain:"demand",editor_decision_required:true},packet=buildContentEditorialReviewPacket([delta],[]),score=Object.fromEntries(editorialReviewCriteria.map((criterion)=>[criterion,4])),decision={review_id:packet.items[0].review_id,preference:"a",scores:{a:score,b:score},semantic_preservation:{a:true,b:false},reviewed_at:"2026-08-29T00:00:00Z",notes:""},output=await buildEditorialDecisionExport(packet,"reviewer fixture",[decision]),validated=validateEditorialReviewDecisions(packet,output);assert.equal(output.reviewer_digest.length,64);assert.equal(validated.decision_count,1);assert.equal(validated.complete,true);assert.equal(validated.human_quality_proven,false);assert.equal(validated.decisions[0].auto_apply,false);await assert.rejects(()=>buildEditorialDecisionExport(packet,"",[decision]),/reviewer identifier/);await assert.rejects(()=>buildEditorialDecisionExport(packet,"reviewer",[]),/at least one decision/);console.log("content editorial review UI export: OK (browser payload accepted by strict importer contract, no auto-apply)");
