import assert from "node:assert/strict";import {buildAcquisitionRemediationPortfolio} from "./acquisition-remediation-portfolio.mjs";import {buildAcquisitionExecutionReadiness} from "./acquisition-execution-readiness.mjs";import {buildAcquisitionApprovalManifest} from "./acquisition-approval-manifest.mjs";const p=buildAcquisitionRemediationPortfolio([{task_id:"a",group_id:"g",keyword:"IT",fulfillment_state:"expansion_required",review_codes:["aio_async_load_not_requested"]}],[{task_id:"a",location_code:2392,language_code:"ja",device:"desktop",os:"windows",depth:10}]),r=buildAcquisitionExecutionReadiness(p,{asOf:new Date("2026-08-29T00:00:00Z")}),m=buildAcquisitionApprovalManifest(p,r,{asOf:new Date("2026-08-29T00:00:00Z")});assert.equal(m.approval_state,"awaiting_explicit_approval");assert.equal(m.maximum_cost.amount,.0012);assert.equal(m.price_valid_until_exclusive,"2026-09-03T00:00:00.000Z");assert.match(m.approval_challenge,/^APPROVE HELIX ACQUISITION [0-9a-f]{16} USD 0\.0012$/);assert.equal(m.execution_authorized,false);assert.equal(m.external_acquisition_triggered,false);const stale=buildAcquisitionApprovalManifest(p,r,{asOf:new Date("2026-09-03T00:00:00Z")});assert.equal(stale.approval_state,"blocked_price_reverification");console.log("acquisition approval manifest: OK (immutable scope, budget, expiry, explicit challenge, no authorization)");
