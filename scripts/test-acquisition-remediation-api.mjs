import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],api=routeResearchApi("/api/v1/acquisition-remediation-portfolio",new URL(`http://localhost/api/v1/acquisition-remediation-portfolio?site_id=${site.site_id}&limit=100`),data,db),s=api.body.summary;assert.equal(api.status,200);assert.equal(api.body.meta.total,98);assert.equal(s.aio_remediation_count,51);assert.equal(s.paa_remediation_count,96);assert.equal(s.combined_remediation_count,49);assert.equal(s.separate_plan_task_count,147);assert.equal(s.avoided_duplicate_task_count,49);assert.equal(s.separate_plan_maximum_cost_usd,.1332);assert.equal(s.maximum_cost_usd,.1038);assert.equal(s.consolidation_savings_usd,.0294);assert.equal(s.submitted_count,0);assert(api.body.data.every((row)=>!row.external_acquisition_triggered&&!row.auto_submission));const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"plan_acquisition_remediation",arguments:{site_id:site.site_id,view:"batches",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,1);console.log("acquisition remediation API/MCP: OK (98 unified tasks, 49 avoided, $0.0294 saved, zero submission)")}finally{db.close()}
