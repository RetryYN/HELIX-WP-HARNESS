import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],o=site.search_contract_code_mapping_oracle;assert.equal(o.summary.mapping_count,1);assert.equal(o.summary.task_count,100);assert.equal(o.summary.fully_echo_verified_task_count,100);assert.equal(o.summary.fully_check_url_verified_task_count,100);assert.equal(o.rows[0].location_code,2392);assert.equal(o.rows[0].language_code,"ja");assert.deepEqual(o.rows[0].check_url_country_codes,["JP"]);assert.deepEqual(o.rows[0].check_url_language_codes,["ja"]);assert.equal(o.rows[0].official_full_code_catalog_proven,false);const url=new URL(`/api/v1/search-contract-code-mappings?site_id=${site.site_id}&state=observed_exact_search_contract_mapping&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db);assert.equal(api.status,200);assert.equal(api.body.meta.total,1);assert.equal(api.body.official_full_code_catalog_proven,false);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"inspect_search_contract_code_mappings",arguments:{site_id:site.site_id,limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,1);assert.equal(mcp.result.structuredContent.external_acquisition_triggered,false);console.log("search contract code mapping API/MCP: OK (100 tasks, Japan 2392 / Japanese ja observed mapping, no full-catalog claim)")}finally{db.close()}
