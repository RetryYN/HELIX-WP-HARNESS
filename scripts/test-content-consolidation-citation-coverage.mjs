import assert from "node:assert/strict";
import {buildConsolidationCitationCoverage} from "./content-consolidation-citation-coverage.mjs";

const claim=(id,sources)=>({claim_id:id,claim_kind:"observed_search_topic",source_claim_ids:sources}),recommendation=(claimId,source,url,domain,score)=>({merged_claim_id:claimId,source_claim_id:source,url,domain,match_score:score}),result=buildConsolidationCitationCoverage([{left_group_id:"a",right_group_id:"b",merged_draft_preview:{claims:[{claim_id:"intro",claim_kind:"editorial_scope",source_claim_ids:["a:i","b:i"]},claim("m1",["a:1","b:1"]),claim("m2",["a:2"])],citation_recommendations:[recommendation("m1","a:1","https://a.example/1","a.example",.4),recommendation("m1","a:1","https://b.example/1","b.example",.2),recommendation("m2","a:2","https://a.example/2","a.example",.6)]}}]);
assert.equal(result.rows.length,2);assert.equal(result.rows[0].coverage_state,"partial_source_lineage_coverage");assert.deepEqual(result.rows[0].missing_source_claim_ids,["b:1"]);assert.equal(result.rows[0].source_lineage_coverage_ratio,.5);assert.equal(result.rows[0].domain_diversity_state,"diverse_domains");assert.equal(result.rows[1].coverage_state,"complete_source_lineage_coverage");assert.equal(result.rows[1].domain_diversity_state,"single_domain");assert.ok(result.rows[1].reason_codes.includes("citation_domain_diversity_insufficient"));assert.deepEqual(result.summary,{claim_count:2,complete_coverage_count:1,partial_coverage_count:1,no_candidate_count:0,diverse_domain_count:1,source_claim_count:3,covered_source_claim_count:2,missing_source_claim_count:1,source_claim_coverage_ratio:.666667,publication_blocked_count:2,auto_approval:false});assert.ok(result.rows.every((row)=>row.audit_digest.length===64&&!row.auto_approval));
console.log("content consolidation citation coverage: OK (source-lineage gaps, domain diversity, publication gate)");
