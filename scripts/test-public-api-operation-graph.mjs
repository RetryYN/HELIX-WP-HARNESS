import assert from "node:assert/strict";import {readFileSync} from "node:fs";import {buildPublicApiOperationGraph} from "./public-api-operation-graph.mjs";
const openapi=JSON.parse(readFileSync("docs/research/evidence/seo-tool-a-openapi.json","utf8")),graph=buildPublicApiOperationGraph(openapi);assert.equal(graph.operation_count,24);assert.equal(graph.operation_ids_unique,true);assert.equal(graph.operation_classification_complete,true);assert.deepEqual(graph.credit_contract_counts,{fixed:12,dynamic:3,zero:9,unclassified:0});assert.equal(graph.operations.filter((row)=>row.lifecycle_role==="register").length,2);assert.equal(graph.operations.filter((row)=>row.authentication_required).length,22);assert.equal(graph.operations.filter((row)=>!row.authentication_required).length,2);assert(graph.dependency_edge_count>=8);assert(graph.operations.every((row)=>row.contract_digest.length===64&&!row.external_request_executed));const volume=graph.operations.find((row)=>row.operation_id==="SearchVolumeHistoryController_register");assert.equal(volume.request_schema,"SearchVolumeHistoryDto");assert.equal(volume.request_fields.find((row)=>row.field==="keywords").max_items,50000);assert.equal(volume.request_fields.find((row)=>row.field==="seoDifficulty").default,false);const rank=graph.operations.find((row)=>row.operation_id==="SearchRankHistoryController_register");assert.deepEqual(rank.request_fields.find((row)=>row.field==="depth").enum,[30,40,50,60,70,80,90,100]);const bulk=graph.operations.find((row)=>row.operation_id==="BulkSiteResearchController_searchBulkSiteResearch");assert.equal(bulk.request_fields.find((row)=>row.field==="urls").max_items,100);assert.equal(graph.external_request_executed,false);console.log(`public API operation graph: OK (24 operations, ${graph.dependency_edge_count} explicit dependencies, complete schema/auth/lifecycle classification)`);
