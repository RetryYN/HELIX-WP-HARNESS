import assert from "node:assert/strict";
import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";
import {routeResearchApi} from "./keyword-dashboard-api.mjs";
import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";

const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const dashboard=projectDashboard(db),site=dashboard.sites[0],rows=dashboard.serp_freshness_signals.filter((row)=>row.site_id===site.site_id);assert.equal(rows.length,100);assert.equal(rows.reduce((sum,row)=>sum+row.timestamped_count,0),462);assert(rows.every((row)=>row.evidence_digest.length===64&&!row.auto_mutation&&!row.external_acquisition_triggered));const api=routeResearchApi("/api/v1/freshness-signals",new URL(`http://localhost/api/v1/freshness-signals?site_id=${site.site_id}&limit=100`),dashboard,db);assert.equal(api.status,200);assert.equal(api.body.meta.total,100);assert.equal(api.body.summary.task_count,100);assert.equal(api.body.summary.insufficient_count,19);assert.equal(api.body.policy,"serp-freshness-distribution.v1");const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"review_serp_freshness",arguments:{site_id:site.site_id,state:"fresh_dominant",limit:100}}},dashboard);assert.equal(mcp.result.structuredContent.summary.task_count,mcp.result.structuredContent.meta.total);console.log(`SERP freshness API/MCP: OK (100 tasks, 462 timestamped top results, ${api.body.summary.insufficient_count} sparse)`)}finally{db.close()}
