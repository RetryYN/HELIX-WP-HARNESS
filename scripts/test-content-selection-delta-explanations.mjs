import assert from "node:assert/strict";import {buildContentSelectionDeltaExplanations} from "./content-selection-delta-explanations.mjs";const candidate=(id,type,score,components)=>({candidate_id:id,content_type:type,text:id,ensemble_score:score,score_components:components}),ensemble={candidate_rows:[candidate("to","title",50,{quality:20,demand:0}),candidate("tn","title",65,{quality:20,demand:15}),candidate("ho","heading",40,{competitive:0,evidence:5}),candidate("hn","heading",60,{competitive:15,evidence:10})],group_rows:[{group_id:"g",main_keyword:"kw",title_selection_changed:true,current_title_candidate_id:"to",recommended_title_candidate_id:"tn",heading_remove_ids:["ho"],heading_add_ids:["hn"]}]},o=buildContentSelectionDeltaExplanations(ensemble),title=o.rows.find((row)=>row.content_type==="title"),heading=o.rows.find((row)=>row.content_type==="heading");assert.equal(o.rows.length,2);assert.equal(o.summary.title_explanation_count,1);assert.equal(o.summary.net_evidence_gain_count,2);assert.equal(title.reason_codes.includes("stronger_demand_history"),true);assert.equal(heading.dominant_gain,"competitive");assert(o.rows.every((row)=>row.explanation_digest.length===64&&row.editor_decision_required&&!row.auto_apply));console.log("content selection delta explanations: OK (component gains/tradeoffs, paired changes, no auto apply)");
