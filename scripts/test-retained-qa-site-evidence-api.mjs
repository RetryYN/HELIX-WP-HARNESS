import assert from "node:assert/strict";
import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";
import {routeResearchApi} from "./keyword-dashboard-api.mjs";
import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";
const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],oracle=site.qa_site_evidence,request=(suffix="")=>{const url=new URL(`/api/v1/qa-site-evidence?site_id=${site.site_id}&limit=100${suffix}`,"http://localhost");return routeResearchApi(url.pathname,url,data,db)},all=request();assert.equal(all.status,200);assert.equal(all.body.meta.total,oracle.summary.observation_count);assert.equal(all.body.full_qa_index,false);assert.equal(all.body.answer_text_retained,false);assert(all.body.data.every((row)=>row.evidence_digest.length===64&&!row.external_acquisition_triggered));const quora=request("&source=quora");assert.equal(quora.body.meta.total,oracle.summary.source_counts.quora??0);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"search_qa_site_evidence",arguments:{site_id:site.site_id,source:"yahoo_chiebukuro",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,oracle.summary.source_counts.yahoo_chiebukuro??0);console.log(`retained Q&A site API/MCP: OK (${oracle.summary.observation_count} observations / ${oracle.summary.keyword_count} keywords / no acquisition)`)}finally{db.close()}
