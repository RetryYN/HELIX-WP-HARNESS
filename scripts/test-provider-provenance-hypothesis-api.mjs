import assert from "node:assert/strict";import {routeResearchApi,researchOpenApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";
const data={sites:[],groups:[]},request=(query="")=>{const url=new URL(`http://localhost/api/v1/provider-provenance?${query}`);return routeResearchApi(url.pathname,url,data)},all=request("limit=100");assert.equal(researchOpenApi.info.version,"2.118.0");assert.equal(all.status,200);assert.equal(all.body.meta.total,24);assert.equal(all.body.summary.internal_stack_proven_count,0);assert.equal(all.body.vendor_confirmation_observed,false);assert.equal(all.body.internal_stack_claim,"not_proven");assert.equal(all.body.external_request_executed,false);const observed=request("state=observed_contract_correspondence&limit=100");assert.equal(observed.body.meta.total,4);const unknown=request("state=provider_unknown&limit=100");assert.equal(unknown.body.meta.total,1);const metadata=request("q=public-search-metadata&limit=100");assert.equal(metadata.body.meta.total,2);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"inspect_provider_provenance_hypotheses",arguments:{state:"retained_transform_reproduction",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,2);const tools=handleMcpMessage({jsonrpc:"2.0",id:2,method:"tools/list"},data).result.tools;assert.equal(tools.length,107);console.log("provider provenance hypothesis API/MCP: OK (API 2.118.0, 24 hypotheses, internal stack proven 0, 107 tools)");
