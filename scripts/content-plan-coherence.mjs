import {createHash} from "node:crypto";
import {tokenizeMatchText} from "./keyword-article-matching.mjs";
import {genericMatchTokens} from "./keyword-policy.mjs";

const digest=(value)=>createHash("sha256").update(JSON.stringify(value)).digest("hex");
const generic=new Set(genericMatchTokens);
const statePriority={ready:2,needs_review:1,blocked:0};
const compareTitles=(left,right)=>(statePriority[right.review?.review_state]??0)-(statePriority[left.review?.review_state]??0)||(right.review?.quality_score??0)-(left.review?.quality_score??0)||(right.review?.evidence_count??0)-(left.review?.evidence_count??0)||Math.abs((left.review?.character_count??35)-35)-Math.abs((right.review?.character_count??35)-35)||left.candidate_id.localeCompare(right.candidate_id);
const meaningfulTokens=(value)=>new Set(tokenizeMatchText(value).filter((token)=>!generic.has(token)));

export function buildContentPlanCoherence(groups,candidates,outlines){
  const outlineByGroup=new Map(outlines.map((row)=>[row.group_id,row]));
  return groups.filter((group)=>group.main_keyword).map((group)=>{
    const titles=candidates.filter((row)=>row.group_id===group.id&&row.content_type==="title"&&row.review?.review_state!=="blocked"&&row.review?.oracle?.evidence_reference_resolved!==false).sort(compareTitles),title=titles[0]??null,outline=outlineByGroup.get(group.id),selectedHeadings=outline?.sections.flatMap((section)=>[section,...section.children])??[],titleEvidence=new Set(title?.evidence_ids??[]),outlineEvidence=new Set(selectedHeadings.flatMap((row)=>row.evidence_ids??[])),sharedEvidence=[...titleEvidence].filter((id)=>outlineEvidence.has(id)),titleTokens=meaningfulTokens(title?.text),headingTokens=meaningfulTokens(selectedHeadings.map((row)=>row.text).join(" ")),sharedTokens=[...titleTokens].filter((token)=>headingTokens.has(token)),evidenceCoverage=titleEvidence.size?sharedEvidence.length/titleEvidence.size:0,lexicalCoverage=titleTokens.size?sharedTokens.length/titleTokens.size:0,averageHeadingQuality=selectedHeadings.length?selectedHeadings.reduce((sum,row)=>sum+(row.review?.quality_score??0),0)/selectedHeadings.length:0,reviewRequiredCount=selectedHeadings.filter((row)=>row.review?.review_state==="needs_review").length,issues=[];
    if(!title)issues.push("title_unavailable");if(!outline||outline.status!=="outline_ready")issues.push("outline_unavailable");if(title&&titleEvidence.size&&evidenceCoverage<.5)issues.push("title_evidence_weakly_represented");if(title&&lexicalCoverage<.5)issues.push("title_outline_lexical_gap");if(reviewRequiredCount)issues.push("selected_heading_review_required");
    const blockers=new Set(["title_unavailable","outline_unavailable"]),blocking=issues.some((issue)=>blockers.has(issue)),score=Math.max(0,Math.round((title?.review?.quality_score??0)*.4+averageHeadingQuality*.35+evidenceCoverage*15+lexicalCoverage*10-(reviewRequiredCount?5:0))),reviewState=blocking?"blocked":issues.length?"needs_review":"ready",base={group_id:group.id,title_candidate_id:title?.candidate_id??null,outline_policy:outline?.policy??null,selected_heading_ids:selectedHeadings.map((row)=>row.candidate_id),quality_score:score,review_state:reviewState,issues,metrics:{title_quality:title?.review?.quality_score??null,average_heading_quality:Math.round(averageHeadingQuality*100)/100,title_evidence_count:titleEvidence.size,outline_evidence_count:outlineEvidence.size,shared_evidence_count:sharedEvidence.length,title_evidence_coverage:Math.round(evidenceCoverage*10000)/10000,title_token_count:titleTokens.size,shared_title_token_count:sharedTokens.length,title_outline_lexical_coverage:Math.round(lexicalCoverage*10000)/10000,selected_heading_count:selectedHeadings.length,selected_heading_review_required_count:reviewRequiredCount},policy:"content-plan-coherence.v1",status:"proposed",auto_approval:false};return{...base,composition_digest:digest(base)};
  });
}
