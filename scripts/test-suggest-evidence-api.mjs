import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/suggest-evidence?site_id=${site.site_id}&q=it%20%E5%B0%B1%E6%B4%BB&mode=prefix&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db),s=api.body.summary;assert.equal(s.source_row_count,10694);assert.equal(s.normalized_candidate_count,10619);assert.equal(s.normalized_duplicate_candidate_count,74);assert.equal(s.duplicate_source_row_count,149);assert.equal(s.volume_conflict_count,20);assert.equal(s.serp_acquired_source_row_count,100);assert.equal(s.serp_unacquired_source_row_count,10594);assert.equal(s.source_sheet_count,15);assert.ok(api.body.meta.total>0);assert.equal(api.body.external_surface_coverage.google,false);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"search_suggest_evidence",arguments:{site_id:site.site_id,query:"it 就活",mode:"prefix",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,api.body.meta.total);console.log(`suggest evidence API/MCP: OK (${s.normalized_candidate_count.toLocaleString()} normalized candidates, source lineage, no surface inference)`)}finally{db.close()}
