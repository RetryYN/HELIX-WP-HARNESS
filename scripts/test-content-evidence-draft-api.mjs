import assert from "node:assert/strict";
import {DatabaseSync} from "node:sqlite";
import {projectDashboard} from "./keyword-dashboard-db.mjs";
import {routeResearchApi,setResearchDb} from "./keyword-dashboard-api.mjs";

const db=new DatabaseSync(".helix/keyword-dashboard.sqlite",{readOnly:true});
try{
  setResearchDb(db);const data=projectDashboard(db),siteId=data.sites[0].site_id,response=routeResearchApi("/api/v1/drafts",new URL(`http://localhost/api/v1/drafts?site_id=${siteId}&limit=100`),data,db);
  assert.equal(response.status,200);assert.equal(response.body.meta.total,63);assert.equal(response.body.summary.draft_count,63);assert.equal(response.body.summary.claim_count,573);assert.equal(response.body.summary.verified_claim_count,0);assert.equal(response.body.summary.claim_with_resolved_evidence_count,510);assert.equal(response.body.summary.evidence_reference_count,2018);assert.equal(response.body.summary.resolved_evidence_reference_count,2018);assert.equal(response.body.summary.unresolved_evidence_reference_count,0);assert.deepEqual(response.body.summary.evidence_type_counts,{competitor_term:1684,serp_demand:316,serp_feature_item:18});assert.equal(response.body.summary.fact_verification_state,"pending_primary_source");assert.equal(response.body.summary.blocked_count,63);assert.equal(response.body.summary.text_export_count,63);assert.equal(response.body.summary.html_export_count,63);assert.equal(response.body.summary.auto_approval,false);
  assert.ok(response.body.data.every((row)=>row.renderer_version==="content-evidence-draft.v1"&&row.review.publication_state==="blocked"&&!row.review.auto_approval&&row.sections.length>=3&&row.claims.length===row.sections.length+1&&row.text&&row.html&&row.revision_digest.length===64&&row.evidence_oracle.all_references_resolved&&row.evidence_oracle.fact_verification_state==="pending_primary_source"&&row.claim_evidence.every((reference)=>reference.resolution_state==="resolved"&&reference.source)));
  const resolved=routeResearchApi("/api/v1/drafts",new URL(`http://localhost/api/v1/drafts?site_id=${siteId}&evidence_state=resolved&limit=100`),data,db),unresolved=routeResearchApi("/api/v1/drafts",new URL(`http://localhost/api/v1/drafts?site_id=${siteId}&evidence_state=unresolved&limit=100`),data,db);assert.equal(resolved.body.meta.total,63);assert.equal(unresolved.body.meta.total,0);
  assert.equal(response.body.provenance.external_acquisition_triggered,false);console.log(`content evidence draft API: OK (${response.body.summary.draft_count} drafts, ${response.body.summary.claim_count} claims, ${response.body.summary.resolved_evidence_reference_count} references resolved, fact verification pending)`);
}finally{setResearchDb(null);db.close()}
