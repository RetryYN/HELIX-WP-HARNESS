import assert from "node:assert/strict";import {assessContentDemandStability} from "./content-demand-stability.mjs";const candidates=[{candidate_id:"stable",group_id:"g",content_type:"heading",heading_level:2,evidence_type:"serp_demand",evidence_ids:["p1"]},{candidate_id:"mixed",group_id:"g",content_type:"title",evidence_type:"serp_demand",evidence_ids:["p1","p2"]},{candidate_id:"other",group_id:"g",content_type:"heading",evidence_type:"competitor_term",evidence_ids:[]}],topics=[{proposal_id:"p1",group_id:"g",topic_kind:"paa",normalized_topic:"a"},{proposal_id:"p2",group_id:"g",topic_kind:"related_search",normalized_topic:"b"}],history=[{group_id:"g",demand_type:"paa",normalized_value:"a",appearance_state:"persistent",display_value:"A",history_digest:"a".repeat(64)},{group_id:"g",demand_type:"related_search",normalized_value:"b",appearance_state:"gained",display_value:"B",history_digest:"b".repeat(64)}],result=assessContentDemandStability(candidates,topics,history);assert.equal(result.rows.find((row)=>row.candidate_id==="stable").demand_stability_state,"stable_observed");assert.equal(result.rows.find((row)=>row.candidate_id==="mixed").demand_stability_state,"mixed_history_review");assert.equal(result.rows.find((row)=>row.candidate_id==="other").demand_stability_state,"not_applicable");assert.equal(result.summary.editor_review_required_count,1);assert(result.rows.every((row)=>row.stability_digest.length===64&&!row.auto_approval));console.log("content demand stability: OK (stable/mixed/volatile/history-missing gates, no auto approval)")
