import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/related-keyword-boundaries?site_id=${site.site_id}&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db),s=api.body.summary;assert.equal(api.body.meta.total,733);assert.equal(s.proposal_occurrence_count,792);assert.equal(s.unique_group_count,677);assert.equal(s.multi_group_source_count,56);assert.equal(s.tie_review_count,18);assert.equal(s.margin_review_count,5);assert.equal(s.clear_leader_count,33);assert.equal(s.review_required_count,23);assert.equal(api.body.auto_assignment,false);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"review_related_keyword_boundaries",arguments:{site_id:site.site_id,review:"required",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,23);console.log("related keyword boundary API/MCP: OK (733 sources, 56 cross-group, 23 reviews, no auto assignment)")}finally{db.close()}
