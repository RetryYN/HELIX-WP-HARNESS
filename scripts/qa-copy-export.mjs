import {createHash} from "node:crypto";
const clean=(value)=>String(value??"").replace(/[\t\r\n]+/gu," ").trim();
const digest=(value)=>createHash("sha256").update(value).digest("hex");
export function buildQaCopyExport(evidence,history){
  const lines=["# HELIX retained Q&A evidence","# scope\tretained_serp_only","# answer_text_retained\tfalse","# full_external_qa_index\tfalse","# absence_confirms_unranked\tfalse","# external_acquisition_triggered\tfalse","","[observations]","source_keyword\tqa_source\trank\ttitle\turl\tcontent_state\tevidence_digest",...(evidence.rows??[]).map((row)=>[row.source_keyword,row.qa_source,row.rank_absolute,row.title,row.url,row.content_state,row.evidence_digest].map(clean).join("\t")),"","[pages]","qa_source\tbest_rank\tkeyword_count\tobservation_count\ttitle\turl\tpage_evidence_digest",...(evidence.pages??[]).map((row)=>[row.qa_source,row.best_rank,row.keyword_count,row.observation_count,row.title,row.url,row.page_evidence_digest].map(clean).join("\t")),"","[appearance_history]","keyword\tqa_source\tstate\tprevious_rank\tcurrent_rank\tprevious_observed_at\tcurrent_observed_at\turl\tevidence_digest",...(history.rows??[]).map((row)=>[row.keyword,row.qa_source,row.state,row.previous_rank,row.current_rank,row.previous_observed_at,row.current_observed_at,row.url,row.evidence_digest].map(clean).join("\t"))];
  const text=`${lines.join("\n")}\n`,base={format:"text/tab-separated-values",observation_count:evidence.rows?.length??0,page_count:evidence.pages?.length??0,history_count:history.rows?.length??0,line_count:lines.length,answer_text_retained:false,full_external_qa_index:false,absence_confirms_unranked:false,external_acquisition_triggered:false,policy:"qa-copy-export.v1"};return{...base,text,export_digest:digest(text)};
}
