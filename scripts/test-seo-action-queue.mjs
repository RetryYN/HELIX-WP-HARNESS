import assert from "node:assert/strict";import {buildSeoActionQueue} from "./seo-action-queue.mjs";
const queue=buildSeoActionQueue({site:{site_id:"s"},groups:[{id:"g",main_keyword:"KW"}],topicCoverage:[{proposal_id:"p",group_id:"g",wp_article_id:1,coverage_status:"missing",evidence_digest:"a".repeat(64)}],topicProposals:[{proposal_id:"p",value:"質問"}],internalLinks:[],serpSignals:[{task_id:"t",group_id:"g",keyword:"KW",signal_types:["visual"],recommended_formats:["original_images"],evidence_digest:"c".repeat(64),evidence:[{evidence_type:"serp_feature",rank_absolute:2}]}],topologyRows:[],drafts:[{group_id:"g",revision:1,title:"記事",revision_digest:"b".repeat(64),review:{publication_state:"blocked",reason_codes:["primary_source_verification_pending"],claim_count:2,verified_claim_count:0}}],rankMonitorPlan:{rows:[],summary:{candidate_count:0}}});assert.equal(queue.rows.length,3);assert.equal(queue.rows[0].priority_band,"P0");const format=queue.rows.find((row)=>row.action_type==="serp_format_review");assert.equal(format.priority_band,"P1");assert.equal(format.priority_reason,"observed_top3_serp_format_not_yet_approved");assert.equal(format.context.best_feature_rank,2);assert.equal(format.context.click_or_demand_inferred,false);assert(queue.rows.every((row)=>row.review_state==="review_required"&&!row.auto_execution&&!row.rank_lift_inferred&&row.action_digest.length===64));console.log("SEO action queue: OK (observed placement priority, evidence-bound workflow, no execution or rank-lift inference)");
