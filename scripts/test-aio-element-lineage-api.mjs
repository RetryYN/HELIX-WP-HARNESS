import assert from "node:assert/strict";
import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";
import {routeResearchApi} from "./keyword-dashboard-api.mjs";
import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";
const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const dashboard=projectDashboard(db),site=dashboard.sites[0],rows=dashboard.aio_element_source_lineage.filter((row)=>row.site_id===site.site_id);assert.equal(rows.length,69);assert.equal(rows.reduce((sum,row)=>sum+row.reference_count,0),168);assert.equal(rows.reduce((sum,row)=>sum+row.link_count,0),9);assert.equal(rows.reduce((sum,row)=>sum+row.image_count,0),8);assert.equal(rows.filter((row)=>row.source_state==="referenced").length,51);assert.equal(rows.filter((row)=>row.source_state==="unreferenced").length,18);assert.equal(rows.reduce((sum,row)=>sum+row.global_reference_mismatch_count,0),2);assert(rows.every((row)=>row.evidence_digest.length===64&&!row.auto_mutation));const url=new URL(`http://localhost/api/v1/aio-element-lineage?site_id=${site.site_id}&limit=100`),api=routeResearchApi(url.pathname,url,dashboard,db);assert.equal(api.status,200);assert.equal(api.body.summary.element_count,69);assert.equal(api.body.summary.element_only_reference_count,2);assert.equal(api.body.summary.review_required_count,2);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"audit_aio_element_sources",arguments:{site_id:site.site_id,review:"required",limit:100}}},dashboard);assert.equal(mcp.result.structuredContent.meta.total,2);assert.equal(mcp.result.structuredContent.summary.element_only_reference_count,2);console.log("AIO element lineage API/MCP: OK (69 elements, 168 references, 2 element-only omissions)")}finally{db.close()}
