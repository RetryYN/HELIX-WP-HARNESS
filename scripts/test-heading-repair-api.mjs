import assert from "node:assert/strict";import {openDashboardDb,projectDashboard} from "./keyword-dashboard-db.mjs";import {routeResearchApi} from "./keyword-dashboard-api.mjs";import {handleMcpMessage} from "./keyword-dashboard-mcp.mjs";const db=openDashboardDb(".helix/keyword-dashboard.sqlite");try{const data=projectDashboard(db),site=data.sites[0],url=new URL(`/api/v1/heading-repairs?site_id=${site.site_id}&limit=100`,"http://localhost"),api=routeResearchApi(url.pathname,url,data,db),s=api.body.summary;assert.equal(api.status,200);assert.equal(api.body.meta.total,180);assert.equal(s.ready_for_editor_review_count,177);assert.equal(s.needs_review_count,3);assert.equal(s.within_observed_iqr_count,177);assert.equal(s.question_pattern_violation_count,0);assert.equal(s.semantic_preservation_verified_count,0);assert(api.body.data.every((row)=>row.semantic_preservation_unverified&&!row.auto_approval&&!row.ranking_effect_inferred&&!row.copying_competitor_heading));assert.equal(api.body.semantic_preservation_verified,false);const mcp=handleMcpMessage({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"review_heading_repairs",arguments:{site_id:site.site_id,state:"ready_for_editor_review",limit:100}}},data);assert.equal(mcp.result.structuredContent.meta.total,177);console.log("heading repair API/MCP: OK (180 repairs, 177 morphology-ready, semantic review required, no copy/approval/rank inference)")}finally{db.close()}
