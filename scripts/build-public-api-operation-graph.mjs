import {readFileSync,writeFileSync} from "node:fs";import {buildPublicApiOperationGraph} from "./public-api-operation-graph.mjs";
const source=JSON.parse(readFileSync("docs/research/evidence/seo-tool-a-openapi.json","utf8")),graph=buildPublicApiOperationGraph(source),output="docs/research/public-api-operation-graph.json";writeFileSync(output,`${JSON.stringify(graph,null,2)}\n`);console.log(`Public API operation graph: ${graph.operation_count} operations, ${graph.dependency_edge_count} explicit edges -> ${output}`);
